# Agent (L3) service dependencies
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2

# LangGraph for state machine
langgraph==0.2.45
langchain-core==0.3.15

# LLM client (OpenAI-compatible for vLLM)
openai==1.54.3

# HTTP client for tool calls
httpx==0.27.2

# Database (for audit log)
psycopg2-binary==2.9.10

python-json-logger==2.0.7
